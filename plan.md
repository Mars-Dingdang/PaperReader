我现在想要做一个论文阅读器项目：实现数学、AI论文的实时翻译，同时保留LaTeX中的公式。

具体的，我希望获得一个APP或者网页，实现：

**功能**

1. 上传一个pdf或者tex文件，对tex类似overleaf一样自动编译，在屏幕的左边展示原始论文的pdf文件；
2. 在屏幕右边，展示将论文正文全部翻译成中文的新pdf（注意是pdf），但是需要保留LaTeX排版、公式、图片、表格等一切其他内容。需要保留引用和超链接。
3. 仿照当前的论文阅读器，实现AI交互功能：文档对话（可以对论文baseline，定理证明等提问，由AI定位文章内容或进行概括）；一键提取亮点（论文的Objective，Contribution，Result，Limitation等）；概念解释。
4. 仿照当前的论文阅读器，实现交互式的引用和跳转：悬浮引用预览（鼠标停留在正文的引用处，直接弹窗显示文献标题、摘要、影响因子，支持一键保存到文献库；使用/ref引用公式和图片也支持预览）。
5. 文献库管理。
6. 阅读模式转换（类似WPS的夜晚模式，深色转换）。

为此你可能需要实现一个软件或者网页。第一步应该做网页简单一点。我的算力如下：

- 交叉信息研究院集群，若干块RTX4090 24G显卡。
- 本地 MacBook Air M4 16+256G; 
- 本地 ThinkBook 16p 2025 U9-275HX+RTX 5070 Laptop 32G+1T.

我应该使用本地模型对LaTeX OCR SFT还是使用现有大模型API？给我一个项目plan.md

# 论文阅读器项目计划书 (plan.md)

## 1. 项目概述
构建一个面向学术论文（尤其是数学与AI领域）的智能阅读与翻译网页应用。用户上传 PDF 或 LaTeX 源码，系统在左侧展示原始排版，右侧展示全文翻译后的新版 PDF（保留公式、图表、引用等全部 LaTeX 元素），并提供 AI 文献对话、亮点提取、概念解释、悬浮引用预览及文献库管理等功能。

## 2. 核心功能拆解
- **双栏 PDF 对比阅读**：左-原文，右-翻译后中文 PDF
- **自动编译与翻译**：`.tex` 文件自动编译；`.pdf` 文件先转 LaTeX 再翻译；翻译过程保持 LaTeX 宏包、公式、表格、交叉引用、超链接原貌
- **AI 交互**：
  - 文档对话（基于全文检索的问答）
  - 一键提取 Objective/Contribution/Result/Limitation
  - 术语/概念解释
- **悬浮引用预览**：鼠标悬停 `\cite{}` 或 `\ref{}` 处弹窗显示文献标题、摘要、影响因子，支持保存到个人文献库
- **文献库管理**：保存、标记、搜索文献
- **深色/阅读模式切换**

## 3. 技术选型建议：本地模型 SFT vs. 云端大模型 API

| 环节             | 方案对比                              | 推荐                                                         |
| ---------------- | ------------------------------------- | ------------------------------------------------------------ |
| **PDF→LaTeX**    | 本地部署 Nougat/LaTeX-OCR 并 SFT      | **混合方案**：PDF 转 LaTeX 对结构化还原要求极高，SFT 需大量标注。推荐**优先使用已成熟的学术 API**（如 Mathpix）处理复杂公式，同时本地部署 Nougat 作为免费备选，并利用 4090 集群对 Nougat 进行领域微调（如数学公式专项优化）以提升自用体验。 |
| **文本翻译**     | 自训练翻译模型 vs. GPT-4/DeepSeek API | **推荐 API**：现代 LLM（DeepSeek-V3、GPT-4o、Claude 3.5）对多语言翻译和 LaTeX 保留已达极高水准，且支持长上下文。直接调用可大幅降低开发成本，避免训练专用 NMT 模型或微调 LLM。成本可控（DeepSeek 翻译每百万 token 约 ¥1）。 |
| **AI 对话/提取** | 本地部署 Llama-3 70B vs. API          | **按场景分层**：简单指令（总结、提取）可用本地部署的轻量模型（如 Qwen-2.5-7B 经量化运行于 5070 笔记本或 4090），降低延迟并保护隐私；复杂推理（多步证明、深度分析）可 fallback 至 GPT-4o API 以获取更强性能。本地模型可用集群进行 LoRA 微调适配学术领域。 |
| **概念解释**     | 同上                                  | 优先使用本地模型+检索增强，离线可用性更好。                  |
| **文献信息获取** | 自建库 vs. 调用开放 API               | **推荐 API**：Semantic Scholar / Crossref / DBLP 免费 API 可直接获取标题、摘要、影响因子等，无需自建大规模文献数据库，仅维护用户个人收藏库。 |

> 结论：**以云端大模型 API 作为主要翻译和高级推理引擎，本地算力用于 PDF 解析（Nougat微调）、轻量级对话模型部署及向量检索，兼顾效果、成本和隐私。**

## 4. 系统架构概览
```
┌──────────────────────────────────────────────────┐
│                   前端 (React/Vue)                │
├───────────────┬──────────────────────────────────┤
│  双栏 PDF 查看│  对话面板 / 亮点提取 / 概念解释   │
│  悬浮引用弹窗 │  文献库管理 / 深色模式            │
└───────┬───────┴─────────────┬────────────────────┘
        │  REST/WebSocket      │
┌───────┴──────────────────────┴────────────────────┐
│               后端服务 (Python FastAPI)             │
├───────────────────────────────────────────────────┤
│ ┌─────────────┐ ┌──────────────┐ ┌─────────────┐ │
│ │ 文件处理服务 │ │ 翻译与编译服务│ │ AI 交互服务  │ │
│ │ - PDF→LaTeX │ │ - 文本抽取    │ │ - 向量检索   │ │
│ │ - LaTeX编译 │ │ - 公式保护    │ │ - 对话引擎   │ │
│ │ - 引用解析  │ │ - 交叉引用修复│ │ - 亮点提取   │ │
│ └──────┬──────┘ └──────┬───────┘ └──────┬──────┘ │
│        │               │                │        │
│ ┌──────┴───────────────┴────────────────┴───────┐ │
│ │                 数据与存储层                   │ │
│ │  - PostgreSQL (用户/文献库)                    │ │
│ │  - 向量数据库 (ChromaDB/Milvus Lite)           │ │
│ │  - 文件存储 (本地/云存储)                      │ │
│ └───────────────────────────────────────────────┘ │
└───────────────────────────────────────────────────┘
         │                  │
         ▼                  ▼
   ┌──────────┐    ┌─────────────────┐
   │本地 GPU  │    │  外部 API        │
   │- Nougat  │    │ - DeepSeek/GPT-4 │
   │- 对话模型│    │ - Semantic Scholar│
   │- 嵌入模型│    │ - Mathpix(可选)  │
   └──────────┘    └─────────────────┘
```

## 5. 关键技术路径

### 5.1 PDF 的 LaTeX 还原与翻译流程
1. **获取源**：优先判断是否为原生 `.tex` 文件；若是 PDF，则进行结构识别。
   - 调用本地部署的 **Nougat**（可微调）将 PDF 转为 LaTeX 源码，重点还原数学公式、表格、章节标题。
   - 对公式密集论文，可混合使用 Mathpix API 提升还原精度（费用约 $0.01/页）。
2. **LaTeX 源码解析**：提取纯文本段落，同时为每个公式、表格、引用标记生成唯一占位符（如 `<EQN_1>`、`<TBL_2>`、`<CITE_key>`），保留所有 LaTeX 命令。
3. **翻译**：将占位符化后的文本发送给 LLM 进行翻译，要求模型保留占位符和 LaTeX 结构。
   - 采用分块翻译（保留上下文），每块包含完整段落与占位符。
   - 利用 LLM 的 few-shot 提示，确保不修改占位符、不翻译公式变量名，保留 `\ref{}`、`\cite{}`、`\url{}` 等。
4. **还原并生成中文 PDF**：将翻译后的文本替换回原 LaTeX 文档，编译生成 PDF。
   - 中文支持需引入 `ctex` 宏包，自动处理字体、换行。
   - 交叉引用与超链接因标签未变，编译后自然保留。
   - 编译工具：服务器安装 TeX Live 完整版，用 `latexmk` 自动化编译。

### 5.2 AI 交互实现
- **文档处理流水线**：
  - 解析 LaTeX/PDF 为结构化文本块（按章节、段落切片），保留元数据（标题、公式 ID、引用键）。
  - 使用本地嵌入模型（如 `BAAI/bge-large-zh`）生成向量，存入 ChromaDB。
- **对话流程**：
  - 用户提问 → 前端→后端检索相关文本块（混合向量+关键词）→ 组装提示词 → 调用 LLM 生成回答。
  - 本地优先使用 Qwen-2.5-7B（运行于 5070/4090），复杂问题路由至 GPT-4o。
- **一键提取亮点**：
  - 提示工程直接向 LLM 索要结构化 JSON，字段包含 Objective, Contribution, Result, Limitation 等，基于前文检索的摘要块生成。

### 5.3 悬浮引用预览
- 解析编译后 `.aux` / `.bbl` 文件，建立引用键到文献条目的映射（或直接从 `.tex` 源码提取 `\bibitem`）。
- 前端通过 PDF.js 渲染 PDF，监听文本选中或悬停事件，识别 `[xx]` 或公式/图表编号。
- 调用 Semantic Scholar API 查询文献元数据（若 bib 文件信息不全），缓存结果。
- 弹窗展示：标题、作者、摘要、期刊/会议、影响因子（来自 Semantic Scholar 或 Journal Citation Reports 缓存）。
- `\ref` 公式/表预览：提取对应的 LaTeX 块渲染为 SVG（用 MathJax/KaTeX）在弹窗中显示。

### 5.4 文献库管理
- 用户可从悬浮弹窗或文档详情页一键保存条目到个人库（存储于 PostgreSQL）。
- 支持手动添加、标签分类、笔记、导出 BibTeX。
- 列表可排序、全文搜索。

### 5.5 深色模式
- 前端 CSS 变量覆盖，深色主题下对左右 PDF 渲染区域可采用 canvas 反色或 PDF.js 的暗色滤镜。

## 6. 开发阶段与里程碑

### Phase 1：基础框架与左侧原文展示（2周）
- 搭建前端项目（React + PDF.js），实现文件上传、拖拽。
- 后端 FastAPI 接收文件，调用本地 LaTeX 编译（对 `.tex`）或直接返回 PDF 给前端展示。
- 完成双栏布局，左栏固定显示原始 PDF。

### Phase 2：PDF→LaTeX 解析与中文翻译（4周）
- 部署 Nougat 模型（微调）到 4090 集群，实现 PDF 转 LaTeX API。
- 开发 LaTeX 源码解析与占位符替换模块。
- 集成 DeepSeek API 翻译，实现文本与占位符的精确保持。
- 实现中文 PDF 编译流水线（ctex + 宏包兼容），右栏显示翻译后 PDF。
- 处理常见编译错误（字体缺失、编码问题），提升成功率。

### Phase 3：AI 交互核心（3周）
- 文档解析与向量索引构建。
- 部署本地对话模型（Qwen-2.5-7B/GPTQ 量化版）到 ThinkBook 5070，实现私有对话。
- 编写亮点提取、概念解释的提示模板与 API。
- 前端聊天面板、一键提取按钮等 UI 组件。

### Phase 4：悬浮引用与文献库（2周）
- 实现引用键提取与 Semantic Scholar 对接。
- 前端悬浮弹窗 UI（Popper.js）。
- 构建文献库后端接口与前端管理页面。
- 公式/图表的 `\ref` 预览（LaTeX 转 SVG）。

### Phase 5：体验优化与深色模式（1周）
- 深色模式 CSS 实现及 PDF 暗色渲染适配。
- 响应式布局、移动端基础支持。
- 性能优化（大 PDF 分块加载、模型调用并发）。
- 用户认证、使用限额（API 成本控制）。

## 7. 算力分配与部署方案

| 资源               | 用途                                                         |
| ------------------ | ------------------------------------------------------------ |
| **4090 集群**      | - 部署 Nougat-base 模型，SFT 微调（单卡或多卡）<br>- 运行 7B/13B 对话模型（可为多用户提供并发） |
| **ThinkBook 5070** | - 本地开发测试、运行量化版对话模型（4-bit 量化 7B 模型）<br>- 运行轻量向量嵌入模型 |
| **MacBook Air M4** | - 前端开发及日常调试，模拟低配场景测试响应速度               |

- 生产环境推荐：将 Nougat 和对话模型部署在 4090 集群上通过内网 API 供后端调用，后端部署在实验室服务器或云主机上。
- 若仅个人使用，可在 ThinkBook 上运行完整服务（Nougat CPU fallback 较慢，可用集群弥补）。

## 8. 技术栈汇总
- **前端**：React + TypeScript + Tailwind CSS，PDF.js / React-pdf，MathJax/KaTeX，Popper.js
- **后端**：Python 3.11+，FastAPI，Celery（异步任务），Redis（消息队列/缓存）
- **数据库**：PostgreSQL（结构化数据），ChromaDB 或 pgvector（向量检索）
- **AI/ML 库**：Hugging Face Transformers，vLLM（高吞吐推理），LangChain（可选，用于检索管道），PyTorch
- **LaTeX 工具链**：TeX Live 2024+，latexmk，ctex 宏包
- **外部 API**：DeepSeek / OpenAI，Semantic Scholar API，Mathpix API（备选）

## 9. 风险与应对
- **PDF 转 LaTeX 质量不足**：通过 SFT 微调 Nougat 改善特定模板，或允许用户上传 `.tex` 优先，PDF 作为辅助。
- **LLM 破坏 LaTeX 结构**：精细化提示工程 + 后处理校验（检查 `\ref`、`\cite` 完整性，编译失败时自动回退重译）。
- **中文编译字体问题**：预装必要中文字体（如思源宋体、黑体），在 Docker 镜像中固化环境。
- **成本控制**：对 API 调用做缓存，相同内容不重复翻译；设置用户每月用量额度。
- **公式交叉引用显示**：利用 PDF.js 的 annotations 或自定义层实现引用跳转预览，技术复杂度较高，可先做文献弹窗，公式/图预览延后。

## 10. 时间线估算（总计约 12 周）
- 阶段 1：第 1-2 周
- 阶段 2：第 3-6 周
- 阶段 3：第 7-9 周
- 阶段 4：第 10-11 周
- 阶段 5：第 12 周及后续迭代

项目计划可根据实际进展进行敏捷调整，优先交付“上传 PDF→双栏翻译”核心闭环，再逐步丰富 AI 交互功能。